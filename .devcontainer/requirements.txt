# Python dependencies installed at runtime via postCreateCommand.sh
#
# Note: Core development tools (numpy, scipy, pandas, matplotlib, jupyter,
# jupyterlab, jupyter-ai, pytest, playwright, ruff, openai, PyYAML, Jinja2,
# requests) are already included in the protodev base image.
#
# Add project-specific or additional packages below. These will be installed
# automatically when the container starts.

# Additional HTTP client (not in base image)
httpx

# Add your project-specific dependencies below:
# scikit-learn
# torch
# transformers
# boto3
# kubernetes
