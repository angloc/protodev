[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
timeout = 300
markers =
    slow: tests that are slow (docker builds, jupyter startup, etc.)
