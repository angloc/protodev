{
  "name": "example-mcp-server",
  "version": "1.0.0",
  "description": "Example MCP server for the protodev dev container",
  "type": "module",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc && chmod +x build/index.js 2>/dev/null || true",
    "watch": "tsc --watch",
    "dev": "npm run build && node build/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18"
  }
}
